const authController = require('./auth')
const verifyController = require('./verify')
const notifyController = require('./notify')
const usersController = require('./users')

module.exports = { authController, verifyController, notifyController, usersController }
